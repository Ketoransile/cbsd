{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node src/server.ts",
    "build": "tsup src/server.ts --format cjs --clean --dts",
    "start": "node dist/server.js",
    "lint": "eslint src/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@repo/contracts": "workspace:*",
    "@repo/utils": "workspace:*"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "ts-node": "^10.9.1",
    "tsup": "^8.0.2",
    "typescript": "^5.3.0"
  }
}
